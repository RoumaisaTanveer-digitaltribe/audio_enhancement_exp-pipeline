# Recommended: Python 3.10 or 3.11 on Windows (pesq + deepfilterlib wheels).
# Python 3.12 may require MSVC Build Tools + Rust to compile native extensions.

# Core audio I/O and analysis
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.11.0

# Quality metrics
pesq>=0.0.4
pystoi>=0.4.0

# System monitoring
psutil>=5.9.0

# Approach 1 — DeepFilterNet3 (CPU PyTorch)
torch>=2.0.0
torchaudio>=2.0.0
deepfilternet>=0.5.6

# Approach 2 — classical signal processing
noisereduce>=3.0.0
pedalboard>=0.9.0
